
### Conv-2026-06-10-Run-v10-Depth4-Walkforward
**Focus Area**: Model Suite
- **Initial Analysis**: Need to find and run `v10_v18_walkforward.py` for the depth-4 model.
- **Action**: Modified `V10_PARAMS` in `scripts/analysis/v10_v18_walkforward.py` to `max_depth=4`.
- **Result**: Walk-forward completed. The depth-4 model failed across the board on the fully OOS walk-forward.
  - **Full Period (2023-07+)**: `V10_alone` at 6bps showed net `-3.4 bps` (LONG) and `-2.5 bps` (SHORT).
  - **Common OOS (2025-07+)**: `V10_alone` at 6bps showed net `-5.1 bps` (LONG) and `-4.1 bps` (SHORT).
  - **Hybrid Variants**: Also failed, yielding strictly negative post-cost edge (`-1.2` to `-8.9` bps).
- **Conclusion**: The depth-4 configuration, despite any potential validation window improvements, does not possess a deployable post-cost walk-forward edge.

### Conv-2026-06-10-Daily-Gatekeeper-Fix
**Focus Area**: Daily Model Gatekeeper & Sentiment Engine
- **Initial Analysis**: The engine reported 99.4% bearish sentiment, and the daily gatekeeper filtering output was skewed due to improper processing.
- **Bug 1 Identified (Z-Scoring on Raw XGBoost Schema)**: When the daily macro scan was moved to the `165-feature daily_xgb schema`, the legacy PyTorch Z-scoring loop was left behind. This caused cross-sectional Z-scoring of raw features for XGBoost, heavily distorting tree splits.
- **Bug 1 Fix**: Removed the `grp_mean`/`grp_std` cross-sectional Z-scoring step from `update_daily_macro_filters` in `orchestrator.py`, directly mapping the correct raw features.
- **Bug 2 Identified (Arbitrary Baseline Drift)**: `Long_Conviction` was calculated via `long_score - short_score` from `rank:pairwise` models. Because pairwise ranker margins are arbitrary and have differing baselines, the raw subtraction created a huge structural negative bias, fooling `market_tracker.py` into thinking the universe was overwhelmingly bearish.
- **Bug 2 Fix**: Mean-centered `long_score`, `short_score` across the cross-section before deriving convictions and multi-timeframe differentials in `model_inference.py`.

### Conv-2026-06-10-Validation-Gauntlet-Remediation-Resumed
**Focus Area**: Model Suite / Validation Gauntlet
- **Initial Analysis**: Resumed execution after a crash during the `daily_xgb` run. Checked the test suite first.
- **Execution & Verification**: Ran the full gauntlet self-test suite (including the slow T8 regression test); all 25 unit/regression tests passed cleanly.
- **Re-Baseline Campaign Completed**: Ran `daily_xgb` on the `daily_5y` dataset under post-remediation rules (anti-overnight checks bypassed for daily bars). Stamped the model metadata with final verdicts (`DEAD`/`DEAD` due to cost drag) and a secure SHA-256 signature. Updated model statistics and project current context.

### Conv-2026-06-10-Signal-Layer-Rectification-Plan (Architect & Auditor: Claude)
**Focus Area**: Model Suite / 1H Signal Generation Strategy → Validation Gauntlet arc
- **Diagnosis**: Established that the recurring 1H model failures were *measurement* failures, not model failures — every historical metric was invalidated downward by a bespoke-harness bug (v8 early-stopping leakage, TBM cost-sign, sniper IBS lookahead, 15m overnight artifact). 1H OHLCV carries ~2–3 bps gross edge vs 6–10 bps costs; architecture search exhausted.
- **Architecture**: Published [[01. Core Architecture/Validation Gauntlet Architecture]] (6-stage pipeline, historical bug classes → automated assertions, 3-tier per-side verdicts, pre-registration + deflated-t, registry stamping + live-engine guard, self-test matrix T1–T8, build plan P0–P7).
- **First audit (post P0–P7 build)**: Core harness verified (reproduced trusted v8 WF: ρ 0.0253/0.0243 vs 0.0261/0.0245). Found 6 critical gaps (A1.1 unwired, hollow overnight guard, missing T8, post-hoc config lock, over-lenient FILTER_GRADE, missing preds.npz) + 9 moderate. Published [[01. Core Architecture/Validation Gauntlet Remediation Plan]] (R1–R8).
- **Second audit (post R1–R8)**: All packages verified faithful; independently re-ran 25/25 tests incl. T8. Verdict evidence reviewed: v8-long FILTER_GRADE is genuine (recent Top-3 WR 52.5% vs universe baseline 48.2%, z=+5.26 — the old "longs weak" conclusion was a 0.5-baseline artifact); v8≡v10 byte-identical results explained (identical metadata specs; the Gauntlet evaluates the spec, not the artifact); `daily_xgb` DEAD/DEAD means the live daily gatekeeper has no validated edge.
- **New critical found & hot-fixed**: registry stamping escapes the GAUNTLET_ROOT test sandbox — the T8 test overwrote v8's production stamp with ephemeral `run_t8_regression`; legitimate R8 stamp (run `20260610T092549Z-5f7d069f`) restored and cryptographically verified. Permanent fix pending: opt-in stamping under test + ledger-existence check in `verify_model_stamp`.
- **Status**: Gauntlet is now the trusted source of truth. `GAUNTLET_ENFORCEMENT` stays `"warn"`; flip to `"enforce"` is a user decision after the stamping fix.

### Conv-2026-06-10-Validation-Gauntlet-Build (P0–P7, archived: P1-Data-Audit, P5, Completion, Remediation notes)
**Focus Area**: Validation Gauntlet implementation
- **P0–P7 Build**: Implemented `scripts/gauntlet/` (contracts, data_audit, leakage, splits, harness, costs, metrics, verdict, report, registry, synth, cli) + `tests/gauntlet/` per the architecture spec; ran the first re-baseline of the 4 core models.
- **R1–R7 Remediation**: Wired A1.1 prefix-invariance into every run (first lookahead certification of the production `compute_features`), rebuilt label-integrity audit (INTRA/UNVERIFIABLE/BOUNDARY classification + anti-overnight statistical check + raw-source verification/waivers), fixed verdict engine (universe-baseline z, pre-registered `primary_k`, exact 12-mo window, perf-decay gate), true pre-registration (lock before Stage 0 + Stage-5 hash verification + started/completed ledger events), `preds.npz` + frozen T8 regression test, `GAUNTLET_ROOT` sandboxing + ledger cleanup + `.gitignore` + AGENTS.md metric-discipline rule + SHA-256 stamps with stale-MD5 rejection, robustness bundle (per-day-vote bar-side inference, real T6 via `validate_fold_plan`, seed threading, `binary_threshold` param, hardened ledger reader, cached device probe).
- **R8 Re-Baseline (final verdicts, config `5f7d069f`)**: `v8_upstox_3y` FILTER/FILTER (run `20260610T092549Z`), `v10_native_1h` FILTER/FILTER (`20260610T093001Z`), `v2_15min_3y` FILTER/FILTER (`20260610T095723Z`), `daily_xgb` DEAD/DEAD (`20260610T102743Z`).
