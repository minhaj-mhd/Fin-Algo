---
title: "Completed Work Archive"
type: log
status: concluded
updated: 2026-06-18
tags: [archive]
---
# 🗃️ Completed Work Archive

> Rolling log of completed Active-Board items, moved here to keep [[06 — Logs/Active Board|Active Board]]
> lean. Durable findings also live as reference docs and in the [[00 — Start Here/Dead-Ends Register|Dead-Ends Register]].

## Active Focus (completed)

* **Performance tracking of candle-stage decisions (COMPLETED — 2026-06-18)**: Closed the performance tracking gap for candle rejections, vetoes, and retracement limits. Updates to `orchestrator.py` record candle confirmation rejections, live reversals, and adverse thrust vetoes under a 1-hour counterfactual tracking window (zero quantity/margin). Retracement limits (`PENDING_LIMIT` orders) that expire without a fill transition to `CANCELLED` status and track counterfactual performance against the original signal price (`market_entry_px`). Terminal outcomes are logged to `data/research/candle_rejections.jsonl` with an asserted 10.0 bps round-trip cost model. Created `analyze_candle_rejections.py` to generate the `Candle Rejection Performance.md` report in the vault. Verified with unit tests. See [[06 — Logs/Conversations/Conv-2026-06-18-Track-Candle-Rejections|Conversation Log]].
* **Side-Specialist Transformer + Cost-Aware Veto Gate (COMPLETED — ❌ sub-significance)**: Tested whether splitting the single-head P(up) transformer into long/short *strength* specialists, used as a **veto on v10's picks**, beats v10-alone. Three arms (OOS 2025-11-28→2026-06-04): (1) ListNet ranker = DEAD (short rank-IC 0.006); (2) custom **cost-aware coverage-budgeted gate loss** `L=−mean(g·n)·1e4+λ·(mean(g)−ρ)²` = the right tool, consistent correctly-signed short-side loss-reduction; (3) **v10-focused gate** (`--v10_restrict`) = best, short Δ vs v10 +2.04/+1.99/+1.62 @K1/3/5. **But no cell clears t>2**; the celebrated short K1 "58%" is RAW WR — net WR @10bps = 51% (coin flip), net +1.83 t=0.38 = noise; sub-cost at 10bps. Loss choice vindicated (user correctly flagged it), but the binding constraint is **information, not loss/architecture** — fourth independent arm pointing to order-flow/microstructure data. **Gemini-veto rescue rejected** (closed MV2 stacking line + unbacktestable search-grounded LLM; forward-test only). Survivorship caveat: fixed 172-name panel. Code committed (`scripts/transformer/{train,eval_sided,gate_veto_v10,make_v10_pickmask}.py`). See [[02 — Models/Transformer/Sided Transformer Preregistration|Pre-registration + full results]] and [[06 — Logs/Conversations/Conv-2026-06-12-Sided-Transformer-Gate|Conversation Log]].
* **15-Minute Conviction-Flip Exit Calculation Fix (COMPLETED)**: Audited the 15m conviction-flip exit and fixed three calculation defects in `orchestrator.py` + `model_inference.py`. (1) Missing/NaN `score_15m` (transient per-ticker 15m fetch rate-limited → batch fallback yields empty frame) was force-closing live positions and mislabeling them `Conviction Flip (15m_score=nan)`; `_check_15m_percentile` now returns an `is_valid` flag and the flip block **holds** (`[CONVICTION-HOLD]`) on indeterminate. (2) The top-X% quantile was computed over only the rate-limit-biased subset of names that carried a score; now requires a valid cross-section ≥ `MIN_15M_UNIVERSE` (30). (3) `score_15m = (l-mean l)-(s-mean s)` subtracted two uncalibrated `rank:pairwise` legs (wider-spread short leg dominated → persistent short bias); new `_combine_long_short` **z-scores each leg** before differencing (applied to 15m/30m/daily, sign preserved). Ruled out the unfitted `scaler.pkl` as a red herring (model trained on raw features via deliberate no-op scaler — raw-at-inference is correct). Residual caveat: the 15m model is genuinely low-signal (WF ρ≈0.058, IBS/Buy_Pressure dominated) — plumbing now correct but signal stays faint; open question whether the flip exit earns its keep (cf. [[06 — Logs/Conversations/Conv-2026-06-11-DualTF-Entry-Exit-Research|Dual-TF research]] — all 15m overlays sub-cost). 62/62 tests pass. See [[06 — Logs/Conversations/Conv-2026-06-12-Fix-15m-Conviction-Flip-Calc|15m Conviction-Flip Fix Log]].
* **Limit Order Fallback for Vanguard Engine (COMPLETED)**: Upgraded the `orchestrator.py` entry execution model. Instead of immediately canceling trades that fail the 15-minute look-back candle confirmation, the engine now generates a `PENDING_LIMIT` order at the 25% retracement strength of the failed candle. If the limit order is not filled within 15 minutes, the engine evaluates the newly completed candle and executes at market price if it confirms the direction; otherwise, it cancels the trade. See [[06 — Logs/Conversations/Conv-2026-06-12-Limit-Order-Fallback|Limit Order Fallback Log]].
* **SignalGenerator `entry_top_k` Hotfix (COMPLETED 2026-06-12 → REGRESSED → RE-FIXED 2026-06-15)**: A `TypeError` crash in the Vanguard Orchestrator from a signature mismatch — `orchestrator.py` passes `entry_top_k` but `generate_candidate_signals` didn't accept it. ⚠️ The 2026-06-12 "fix" targeted the **old monolithic** `signal_generator.py` and did **not** survive: the later refactor `c6b4c40` ("split monolithic signal engine into modular vanguard package") rebuilt it as `signal_generation.py` *without* the plumbing (`git log -S"entry_top_k"` confirms it never existed in that file), so the live engine crashed again. Re-fixed 2026-06-15: added `entry_top_k=2` to the signature and replaced both hardcoded `head(2)` selections (rank-net + raw-directional) with `head(entry_top_k)`; live engine drives it from `config.ENTRY_TOP_K = 5`. Behavioral effect: AI pipeline funnel widens from 4 → up to 10 candidates/side (pre-gate). See [[06 — Logs/Conversations/Conv-2026-06-15-Fix-SignalGenerator-entry_top_k-Regression|Re-Fix Log]].
* **V2 30-Minute Model Build & Gauntlet Certification (COMPLETED)**: Built a replacement `v2_30min` model (`v2_30min_v3_3y`) trained on 42 months of native Upstox V3 30-minute historical data. Applied the explicit `session_masked_fwd()` overnight label drop logic in the data pipeline to sever intraday labels from the market close gap. The Gauntlet mathematically verified the structural fix: the overnight boundary match rate dropped to `0.43%` (well below the 1.0% limit), proving that label leakage is cured. The model cleared a strict 6-fold walk-forward evaluation out-of-sample, demonstrating highly consistent positive edge (+0.0282% Long, +0.0198% Short per bar). The Gauntlet officially awarded the model a **`FILTER_GRADE`** stamp for both sides. See [[06. Context & Logs/Conversations/Conv-2026-06-11-V2-30Min-Model|V2 30-Min Model Build Log]].
* **30-Minute Model Gauntlet Evaluation (ABORTED)**: Attempted to run the Validation Gauntlet on the legacy `v1_30min` model. The Gauntlet aborted hard at Stage 0 (Data Audit) with an assertion failure: `Overnight label leakage detected! Match rate 99.99% exceeds 1% limit.` This means the 30-minute training dataset (`ranking_data_upstox_30min_1y.csv`) suffers from the same overnight boundary leakage bug that previously afflicted the 15-minute models. The labels cross the session, causing the intraday model to learn overnight gap predictions. Per Engineering Discipline Rule 3, the assertion was not weakened. The `v1_30min` model is structurally void and its prior metrics are invalid. See [[06. Context & Logs/Conversations/Conv-2026-06-11-Gauntlet-30-Min-Model|Conversation Log]].
* **Dual-TF Entry/Exit Overlay Research (COMPLETED — NEGATIVE, with one new structural finding)**: Exhaustively tested whether the 15m conviction ranker (`v3_15min_clean`) can dictate entry, blocking, or exit for 1h top-3 host signals (`v10_native_1h`) — 5 studies, 6-fold purged WF OOS 2024-09→2026-06, ~13k trades, all exploratory (no Gauntlet runs consumed). **Verdict: full-hold is optimal; every overlay (entry rank-trajectory, conviction-decay exit, asymmetric "let winners run", conviction-momentum×price buckets) is sub-cost or inverted by mean reversion.** Entry-slope "signal" formally retracted (sign flips across cuts). ⭐ New finding: the residual mean reversion **concentrates at EOD** — hour-13 host trades that dipped by +90min bounce +6.7 bps long (p=0.0037) / +8.9 bps short (p=0.0066) in the 14:45–15:15 window — coherent both sides but still < 10 bps binding cost; only execution-cost reduction or new features (options OI/IV, order-flow) could flip it. Reusable asset: `data/research/entry_exit/dualtf_trade_panel.csv` (13,020 trades × full 15m context — tests any new rule in seconds, no retraining). See [[04 — Research/Dual-TF Entry-Exit Overlay Research|Findings Report]] and [[06 — Logs/Conversations/Conv-2026-06-11-DualTF-Entry-Exit-Research|Conversation Log]].
* **Streaming LLM Response for Ticker Display (COMPLETED)**: Upgraded the ticker intelligence UI to stream the Gemini LLM response in real-time instead of waiting for the full response with a loader. Added `stream_ticker_analysis` in the backend using `generate_content_stream` and implemented a partial JSON regex parser in the frontend using the Fetch API `ReadableStream` to populate the report fields incrementally. See [[06 — Logs/Conversations/Conv-2026-06-11-Ticker-Streaming|Ticker Streaming Log]].
* **Immediate Look-back Candle Entry & Percentile Recalibration (COMPLETED)**: Transformed the entry execution model from a 15-minute pending confirmation wait to an on-the-spot look-back candle check. Implemented custom wick-position rules (LONG check permits bearish candle if close is in the top 40% of range; SHORT check permits bullish candle if close is in the bottom 40% of range). Removed the legacy `PENDING_ENTRY` state and loop. Swapped daily macro gatekeeper to `daily_macro_v2`. Replaced absolute 0.08 conviction and raw score thresholds with scale-free top-K rank entry (top-5) and holding extension checks (at or above p95 of current scan) in `signal_generation.py` and `orchestrator.py`, resolving signal starvation and conviction ergonomics permanently. Verified via custom unit tests and test suite (62/62 tests passing). See [[06. Context & Logs/Conversations/Conv-2026-06-11-Immediate-Candle-Entry|Immediate Look-back Entry & Percentile Recalibration Log]].
* **Dominance & Variance Channel Analysis (COMPLETED)**: Ran decisive tests on certified out-of-sample predictions (`preds.npz`) for 1H and Daily models. Dominance strongly correlates with realized outcome dispersion ($p < 0.001$), but positively (higher dominance = higher realized volatility/dispersion). Shannon entropy acts as an inverse volatility proxy. Cross-model agreement reduces outcome dispersion (Spearman agreement $r = -0.24$ with absolute return, $p \approx 0.0$). Standardized sizing is retained; dominance-based sizing idea set down. See [[06. Context & Logs/Conversations/Conv-2026-06-11-Dominance-Variance-Sizing|Conversation Log]] and [[04 — Research/Dominance Variance Analysis|Dominance & Variance Analysis Report]].
* **Meta-Veto MV2 (LINE CLOSED PERMANENTLY — Clean Campaign Completed)**: Full MV2 decontamination campaign completed. Genuine step=2 walk-forward runs executed for v8_upstox_3y and v2_15min_3y (both sides FILTER_GRADE). Clean candidate panel built (G1 OK: 17 DEV months, 22,206 trades, zero lookahead backfill). Orthogonality audit M1 passed (max |partial IC| = 0.0576). dev_run capacity ladder halted at Rung 3 (GBM -5.11 bps failed to beat Logistic -5.10 bps by >= +0.5 bps). Winner Logistic has negative kept-net (-5.10 bps); freeze.py pre-check failed G2 DEV-promise gate and aborted. **Re-run on v10-v19 predictions** built a panel with 23,506 DEV trades, passed M1 orthogonality audit (max |partial IC| = 0.025839 on `daily_v3_sent`), but the best meta-model (Rung 3 MLP NN) still ended up net-negative at `-5.09 bps` kept-net, failing the G2 pre-check and aborting winner freezing. The price/volume/macro stacking line is permanently closed. See [[02 — Models/Meta-Veto/Meta-Veto Rectification Plan MV2|Rectification Plan]], [[06. Context & Logs/Conversations/Conv-2026-06-10-Meta-Veto-MV2-Clean|Conversation Log]], [[06 — Logs/Conversations/Conv-2026-06-11-Gauntlet-Test-V10-V19|v10-v19 Stacking Log]], and [[02 — Models/Meta-Veto/M1 Orthogonality Audit MV2|M1 Audit Report]].
* **ORIGINAL Meta-Veto Stacking Framework Spec (SUPERSEDED by MV2)**: [Retained for reference] After the gating sweeps closed the binary-gate line, architected the combination layer. Replaced by the MV2 plan due to first build violations. See [[02 — Models/Meta-Veto/Meta-Veto Stacking Framework Plan]] and [[06. Context & Logs/Conversations/Conv-2026-06-10-Meta-Veto-Framework|Conversation Log]].
* **Daily Gatekeeper V2 & V3 Rebuild (COMPLETED & CERTIFIED)**: Fully completed the daily gatekeeper rebuild plan. Trained and certified `daily_macro_v2` (3-day label) and `daily_macro_v3` (1-day label) models. Certified both standalone models in Gauntlet v2. Evaluated out-of-sample gating uplift on `v8_upstox_3y` (1H) and `v2_15min_3y` (15M) trades. ⚠️ **A "last-week backtest" (2026-06-01→08, +0.296%/+0.722% avg returns) was struck from this record on audit (Claude, 2026-06-11): the registry artifact was trained on data through 2026-06-08, making the window 100% IN-SAMPLE; n=36 trades over 6 days is statistically void; the short basket's +0.72%/trade in a falling week (market −0.10%/day) is beta, not selection; and v3's 1-day short is not retail-tradeable (overnight cash-equity short). Per Metric Discipline rule 2, ad-hoc backtests hold no verdict authority — these numbers are ⚠️ UNVERIFIED and must not be quoted as benchmarks.** The authoritative v3 numbers remain its Gauntlet stamp (long DEAD / short FILTER_GRADE, run `20260610T144343Z-5f7d069f`). See [[02 — Models/Daily Gatekeeper/Daily Gatekeeper V2 Rebuild Plan]], [[02 — Models/Daily Gatekeeper/Daily Gatekeeper V2 Rebuild and Certification Report]], and [[06 — Logs/Daily Logs/2026-06-11|Daily Log 2026-06-11]].
* **Cross-Sectional Transformer Proposal (❌ KILLED at Stage 0, 2026-06-11)**: The pre-registered Stage-0 falsification was run (`scripts/analysis/stage0_leadlag_test.py`, exploratory): adding the lead-lag block (lagged universe-mean return 1/2/4 bars, dispersion, breadth, Nifty500 lags) to the v10 spec under 9-fold purged WF gave **Δρ = −0.0028 long / −0.0007 short** (BASE +0.0260/+0.0258 → AUG +0.0232/+0.0251). Kill-bar (Δ≥+0.01) not met — **cross-name lead-lag info is absent at 1h granularity; do not build the transformer.** The way-through to a better 1h ranker is now confirmed to be **order-flow/microstructure data acquisition**, not architecture (all modeling/recombination paths — meta-veto, dual-TF, CST — are closed). Result: `data/research/stage0_leadlag_result.txt`. Original spec retained below for reference. Authored the full research spec for the post-tree-era architecture candidate: a small (≤1M-param) transformer with attention *across* the 172-ticker universe per timestamp (temporal GRU encoder per name + ticker/sector/ToD embeddings + global market token), listwise ranking loss on cross-sectional z-scored forward returns. Includes pre-registered kill criteria (raw ρ ≥ 0.05, non-decay, must beat a no-attention ablation, worst-of-5-seeds) and a staged plan whose **Stage 0 is a 1-day falsification test**: add cross-asset/lead-lag features (lagged Nifty futures, sector returns, breadth, peer returns) to the existing XGBoost ranker — if raw ρ doesn't move above 0.026, the CST is dead on arrival. All numbers ⚠️ UNVERIFIED (proposal only). See [[02 — Models/Transformer/Cross-Sectional Transformer Architecture Proposal]] and [[06. Context & Logs/Conversations/Conv-2026-06-10-Next-Architecture-Advisory|Conversation Log]].
* **1H Model Signal Diagnosis (COMPLETED)**: Diagnosed why the 1-hour model (`v8_upstox_3y`) is generating very low signals in live trading. The core issue is a mathematical mismatch between the hardcoded `0.08` conviction and raw score thresholds in `config.py` / `risk_manager.py` and the actual distribution of predictions from `v8_upstox_3y` (max Long Conviction is `0.067`, and all raw scores are negative). See [[06. Context & Logs/Conversations/Conv-2026-06-10-1H-Model-Low-Signals|Conversation Log]].
* **Validation Gauntlet Remediation (R1–R8 COMPLETED, RE-BASELINED & AUDIT-PASSED)**: Fully executed the Remediation Plan to resolve all 6 critical and 9 moderate gaps in the self-testing evaluation harness. Fixed prefix-invariance wiring (R1), label-integrity check (R2), verdict engine baseline & prereg (R3/R4), T8 regression tests & `preds.npz` export (R5), path sandboxing & registry SHA-256 signatures (R6), and robustness fixes (R7). Completed the R8 Re-Baseline Campaign: `daily_xgb` → `DEAD`/`DEAD` (⚠️ the live daily gatekeeper has no validated edge — recent z only 0.4/1.0); `v8_upstox_3y`, `v10_native_1h`, `v2_15min_3y` → `FILTER_GRADE` both sides. **Second conformance audit (Claude)**: R1–R7 verified faithful; 25/25 tests independently re-run and passing incl. T8; v8-long FILTER_GRADE confirmed evidence-backed (recent WR 52.5% vs universe baseline 48.2%, z=+5.26); v8≡v10 identical results explained (identical metadata specs — the Gauntlet evaluates the spec, not the artifact). **One new critical found & hot-fixed**: registry stamping escapes the test sandbox — the T8 test overwrote v8's production stamp with an ephemeral test run-id; legitimate R8 stamp restored. Permanent fix pending (stamping opt-in under test + ledger-existence check in `verify_model_stamp`). `GAUNTLET_ENFORCEMENT` remains `"warn"`. See [[06 — Logs/Daily Logs/2026-06-10|Daily Log 2026-06-10]] (Gauntlet arc archived there).
* **Gauntlet Tests for v8–v19 Models (COMPLETED)**: Executed a batch testing pipeline to run the Validation Gauntlet sequentially across all models from `v8_upstox_3y` up to `v19_catboost_1h`. Supported models (`v8_upstox_3y`, `v9_clean_1h`, `v10_native_1h`, `v10_depth4_1h`, `v11_utility_1h`, `v12_lambdamart_1h`, `v13_ndcg_raw_1h`, `v14_lambdamart_no_es_1h`, `v15_lambdamart_es_1h`, `v16_binary_breakout_1h`, `v19_catboost_1h`) generated official verdicts of `FILTER_GRADE` or `DEAD`. Models `v15_lambdamart_map5_1h` failed due to missing metadata, and Random Forest models `v17` & `v18` timed out under evaluation complexity. Evaluation reports were extracted separately to `08. Model Analysis/Gauntlet Reports/`, and tables in [[02 — Models/_Shared/Model Performance & Statistics]] were updated accordingly. See [[06 — Logs/Conversations/Conv-2026-06-11-Gauntlet-Test-V10-V19|Conversation Log]].
* **Gemini 3 Model Upgrade**: Migrated the core Vanguard engine from the heavily rate-limited `gemini-2.5-flash` to the new Gemini 3 series. Stage 1 (Technical Veto) now uses `gemini-3-flash-preview` (with `3.5-flash` as backup) to maximize raw speed. Stage 2 (News/Search Veto) strictly uses `gemini-3.5-flash` because the 3.0 preview model fails instantly on the free tier when Google Search Grounding is enabled. See [[06. Context & Logs/Conversations/Conv-2026-06-10-Model-Upgrade|Conversation Log]].
* **Live Engine Model Swapping (COMPLETED)**: Upgraded the active signal generator to `v10_native_1h` (depth-5), daily macro gatekeeper to `daily_macro_v3` (v3 daily), and the 15-minute dashboard model to `v3_15min_clean`. All models successfully verified under the Validation Gauntlet and initialized on CUDA. See [[06. Context & Logs/Conversations/Conv-2026-06-11-Engine-Model-Replacement|Conversation Log]].
* **v8 1-Hour Model DEMOTED (Walk-Forward Reanalysis)**: ⚠️ **CRITICAL**: Re-ran `v8_upstox_3y` (the "Active Production Champion" 1H ranker) through a genuine 9-fold purged walk-forward (`scripts/analysis/v8_walkforward.py`, 320,931 OOS rows, 2023-08→2026-05). The saved artifact's 0.046/0.049 Spearman came from a single 80/20 split where the test set doubled as the early-stopping eval set (leakage). Under genuine WF: average Spearman ≈ 0.026/0.024, **monotonically decaying to ~0.012/0.004 in the most recent fold (2026-04/05)**. Top-1/Top-3 Long & Short are **net-negative at 6bps** (−3.1 to −4.3 bps, t=−2.0 to −4.2) across the full span and the last 12 months. Cost-sign verified correct (not a TBM-style bug — the raw edge is just too thin and decaying). No time-of-day pocket survives costs. **v8 should not be treated as deployable; do not rely on its reported metrics.** This is consistent with the V10 Depth-4 result below — v10 (depth-4 sibling architecture) is also net-negative (-3.4/-2.5bps). 15m still needs the same re-audit. **Corroboration**: a separate static 70/10/20 chronological split (`scripts/analysis/v8_static_70_10_20.py`, val≠test early stopping, test window ≈ original's) reproduces the same ~0.02 Spearman (not 0.046/0.049) with `best_iteration` collapsing to 37/13 of the 500-round budget — confirming the original's higher numbers came from early-stopping-on-test-set leakage, not the rolling-WF harness. See [[02 — Models/_Shared/Model Performance & Statistics|Model Performance & Statistics]] and [[06. Context & Logs/Conversations/Conv-2026-06-10-V8-Walkforward-Reanalysis|Conversation Log]].
* **Raw-Signal-Only Audit (v8 vs v10-depth4)**: Following the v8 demotion, computed raw cross-sectional Spearman for `v10_native_1h` (depth-4) directly from existing WF predictions (`data/model_analysis/v10_v18_independent/walkforward_preds.npz`): ρ=+0.0273/+0.0251 avg, decaying to +0.0129/+0.0049 in the latest fold — essentially identical to v8's +0.026/+0.024, and highly significant (paired t-test t=8.67/6.77, p<0.0001). Raw Top-3 win-rate z-tests show the **short side carries the only persistent real signal** (v8 z=5.4, v10 z=7.3, v10 short still significant in the last 12mo at z=3.8); longs are weak/decayed. Neither survives 6bps costs standalone, but the short-side score is statistically real enough to use as a **filter/feature** (vs. TBM/v18/v19 which have zero raw signal, AUC~0.50). `v2_15min_3y`/`v1_30min` still need this same raw-signal check — see [[02 — Models/_Shared/Model Performance & Statistics|Model Performance & Statistics]] section "Raw-Signal-Only Audit".
* **Gemini Rotator Rate Limit Overhaul**: Completely overhauled the `GeminiRotator` in `gemini_client_manager.py` to intelligently handle `429 RESOURCE_EXHAUSTED` responses. Previously, the engine would blindly sleep for 3s and rotate through all keys until they were exhausted, resulting in skipped trades. The rotator now parses the `retryDelay` directly from the Gemini API exception, maintains a strict `key_available_time` registry, and gracefully pauses execution for the exact duration required if all keys are simultaneously on cooldown. See [[06. Context & Logs/Conversations/Conv-2026-06-10-Gemini-Rate-Limit-Fix|Conversation Log]].
* **Daily Gatekeeper & Engine Sentiment Fix**: Fixed a critical bug in `orchestrator.py` where legacy PyTorch cross-sectional Z-scoring was corrupting the raw features fed to the `daily_xgb` gatekeeper model. Additionally, fixed a baseline drift issue in `model_inference.py` by mean-centering the raw XGBoost `rank:pairwise` margins before calculating `Long_Conviction`, restoring accurate and balanced Engine Sentiment metrics (preventing the 99.4% bearish skew). See [[06. Context & Logs/Conversations/Conv-2026-06-10-Daily-Gatekeeper-Fix|Conversation Log]].
* **V10 Depth-4 Walk-Forward Validation**: Executed a full rolling walk-forward test for the V10+V18 architecture restricted to `max_depth=4`. Discovered that despite potential validation window improvements, the depth-4 configuration does not possess any deployable post-cost walk-forward edge (yielding net `-3.4 bps` Long and `-2.5 bps` Short over the full period at 6bps cost). Confirms that restricting depth does not save the ranking model's edge under strict OOS conditions. See [[06 — Logs/Daily Logs/2026-06-10|Daily Log 2026-06-10]].
* **Triple Barrier Method (TBM) Validation**: Designed and executed a comprehensive Triple Barrier Method data labeling and walk-forward validation pipeline, shifting the target from end-of-bar returns to true intra-bar sequential barrier outcomes. ⚠️ **CORRECTED:** the initial SHORT "breakthrough" (+5.18 bps/trade, PF 1.25) was a **cost-sign harness bug** (net cost added instead of subtracted). After the fix, both sides are net-negative post-cost — true SHORT ≈ 50.7% raw / 44.85% net@6bps / +0.63pp skill, LONG dead. **TBM 1h has no deployable post-cost edge.** Always validate RAW vs NET WR per side. See [[06 — Logs/Daily Logs/2026-06-09|Daily Log 2026-06-09]] and [[04 — Research/TBM 1h Ensemble Results]].
* **Holiday and Weekend Trading Check**: Added `NSE_HOLIDAYS_2026` to `config.py` and implemented checks in `orchestrator.py` inside the `run()` loop and `update_daily_macro_filters()`. The engine now prevents trading and unnecessary API calls on market holidays and weekends by checking the date on start and at the beginning of each execution cycle. See [[06 — Logs/Daily Logs/2026-06-09|Daily Log 2026-06-09]].
* **Jupyter MCP Clean Start Architecture**: Removed all brittle wrapper scripts (`run_mcp_jupyter.bat`) and environment variable hacks. The architecture is now rebuilt on a clean foundation: `uvx mcp-jupyter` runs natively in the MCP config, and the local Jupyter Notebook backend is explicitly launched with `--ServerApp.token="" --ServerApp.password=""` on port 8888. This completely bypasses the 403 Forbidden token authentication issues over the local loopback. See [[06 — Logs/Daily Logs/2026-06-09|Daily Log 2026-06-09]].
* **Track Skipped AI Trades**: Modified `orchestrator.py` to route trades skipped due to Gemini API `SYSTEM_ERROR` into `start_vetoed_tracking()`. This ensures that even when the AI layer is entirely unavailable, the engine tracks what the trade's performance *would* have been, allowing for continuous model performance monitoring. See [[06 — Logs/Daily Logs/2026-06-09|Daily Log 2026-06-09]].
* **Gemini 503 Spike Handler**: Resolved an issue where temporary `503 UNAVAILABLE` errors from the Gemini API would cause the rotator to rapidly burn through all keys in <1 second and skip trades. Increased max attempts to `keys * 3` and added a 3-second sleep between rotations in `gemini_client_manager.py` to ride out temporary demand spikes. See [[06 — Logs/Daily Logs/2026-06-09|Daily Log 2026-06-09]].
* **Gemini Rotator Update**: Refactored the `GeminiRotator` in `gemini_client_manager.py` to exclusively rotate through main keys on failure instead of using a fallback backup key, and locked the model globally to `gemini-2.5-flash` in `config.py`. See [[06 — Logs/Daily Logs/2026-06-09|Daily Log 2026-06-09]].
* **Hybrid Model V10 + V16 Optimization**: Built and evaluated a Hybrid architecture utilizing the v10 (Ranking) and v16 (Binary Breakout) models. Discovered that restricting tree `max_depth` from 5 to 4 dramatically improved out-of-sample generalization, increasing the Top 1 Short edge from +3.4 bps to +7.82 net bps per trade. Additionally confirmed that dropping the conviction threshold to force high-volume trading (e.g., 57% Prob, Top 3) destroys edge due to the 10 bps statutory equity fee structure. See [[06 — Logs/Conversations/Conv-2026-06-09-Hybrid-Optimization|Conversation Log: Hybrid Model V10 + V16 Optimization]].
* **v19 CatBoost Architecture Test**: Implemented CatBoost (Oblivious Trees) as a potential solution to the v18 Random Forest OOS collapse. The walk-forward evaluation proved definitively that the binary directional target (`> 0 bps`) yields zero predictive edge regardless of model architecture. CatBoost achieved ~0.50 AUC, confirming the absolute target cannot be predicted by the current feature matrix. See [[06 — Logs/Daily Logs/2026-06-09|Daily Log 2026-06-09]].
* **1H XGBoost Edge Diagnosis**: Reconfirmed why repeated public-data 1-hour XGBoost versions can fail to produce deployable edge: insufficient calendar-regime diversity, tiny raw rank correlations, same-bar feature evaluation leakage (especially IBS), transaction-cost erosion, and globally averaged thresholds hiding time-of-day pockets. Practical next architecture is a diversified model panel with separate long/short rankers, regime gates, time-of-day specialists, and strict completed-bar walk-forward validation. See [[06 — Logs/Daily Logs/2026-06-08|Daily Log 2026-06-08]].
* **Sniper Trades Teardown & Execution Constraints**: Completely removed Pipeline 3 "Vanguard Sniper Waterfall Logic" from `signal_generation.py`, stripped out related configuration thresholds from `config.py`, and removed specialized `is_vanguard` execution overrides from the shadow tracker (`orchestrator.py`). Deleted the generated 3 Sniper trades from the live SQLite database. Additionally, modified the main execution loop to strictly prevent any structural or AI trades from being taken before 10:15 AM. Added persistent success/fail telemetry for Gemini API keys in `gemini_client_manager.py`.
* **Model Edge Audit & ToD Profiling**: Ran a comprehensive multi-model Top-3 evaluation across `v8_upstox_3y`, `v10_native_1h`, and `v2_15min_3y` on May 2026 holdout data. Discovered that the 1H v8 model maintains its dominance exclusively at the 14:30 bar (66% Long WR, +64 bps edge; 64% Short WR, +47 bps net drop). The clean v10 model fails to consolidate extreme conviction. The new v2 15-minute model exhibits extreme structural alpha strictly in specific time-of-day pockets (14:00 Long WR = 73.6%, 14:15 Short WR = 71.9%), warranting a Hybrid Time-of-Day Execution Engine.
* **15-Minute Model Suite Upgrade (3-Year Standard)**: Successfully upgraded the 15-minute XGBoost models to comply with the 3-year data mandate to prevent regime overfitting. Engineered a new robust data pipeline to fetch 1-minute historical data from Upstox in 30-day chunks and resample to 15-minute intervals, overcoming API constraints. Trained new standalone `v2_15min_3y` models using 6-fold dynamic walk-forward validation across 3.1 million rows. Final validation edge achieved: +4.7 bps per bar with ~58% Long Top-3 Win Rate. Models registered to the Model Registry.
* **Vanguard Sniper Strategy Backtest & Bugfix**: Fixed a critical sizing multiplier bug in the Vanguard sniper engine that caused a 2% portfolio loss by stripping duplicate `5.0x` scaling factors from `signal_generation.py`. Additionally, generated and executed a Jupyter backtest of the sniper tiers on May 2026 data. The backtest revealed a structurally negative edge for short-side Tiers A, B, and C (Tier B had a -214% return and massive drawdown), while the long-side Tier D performed well (+142%). Recommended pausing all short-side sniper execution until retuned.
* **Sniper Strategy Fresh OOS Verification (by Claude, 2026-06-07)**: Ran a clean, no-lookahead backtest on Jan–May 2026 data (provably outside the training set) to independently verify the research claims. Key findings: (1) The edge is REAL — Tier B returned +17.2% in 5 months (~41% annualised) on truly unseen data. (2) The research's 68–76% WR claims are achievable only with intrabar IBS lookahead from the 14:30 bar; the practically tradeable version (13:30 bar signal → enter at 14:30) yields 55–60% WR. (3) Tier C (Inverted Long, L<-0.167) is structurally unavailable with a 1-bar lag — the model only generates extreme negative long scores at the 14:30 bar itself. (4) Feb 2026 was the only losing month, confirming the Macro Regime Filter is non-optional for short strategies. Full analysis: [[02 — Models/1H/Fresh OOS Verification - Jan-May 2026]].
* **Sniper Strategy Live Configuration Finalised (by Claude, 2026-06-07)**: Root-caused the research vs backtest WR gap as IBS evaluation leakage (not training leakage — the model is clean). Confirmed the strategy is production-ready. Finalised exact live configuration: signal from completed 13:30 bar, entry at 14:30 open, hard exit at 15:10 IST (MIS buffer), macro gate mandatory (Nifty50 vs 200-SMA). Tier A max size, Tier B standard size, Tier D half size. Tier C structurally dead with EOB architecture. Full config: [[02 — Models/1H/Live Trading Configuration & Verdict]].
* **Vanguard Sniper Engine Audit & Concurrency Fixes**: Conducted a systematic audit of the merged engine pipelines. Modified `start_shadow_trade` to support immediate execution for Vanguard Snipers, entirely bypassing the 15-minute candle confirmation boundary. Added runtime conviction flip check bypasses in the scan loop, and disabled time-extension holding windows to strictly cap leveraged sniper positions at 1 hour. Resolved a test-suite concurrency failure in `tests/test_orchestrator_concurrency.py` by filtering rate-limiting sleep mocks, restoring a 100% test-pass rate.
* **Fix Upstox Rate Limits & Pandas Fragmentation**: Resolved Upstox Historical Data Cloudflare 429 Rate Limiting by throttling concurrent fetches from 15 to 5 workers and adding a 0.3s delay in `orchestrator.py`. Simultaneously resolved widespread Pandas "DataFrame is highly fragmented" warnings in `model_inference.py`, `orchestrator.py`, and `persistence.py` by refactoring repetitive `.assign()` chains using `pd.concat()` and appending `.copy()` correctly.
* **Fix Live Engine Pulse Scan Hang**: Identified and resolved a critical bug where the live Vanguard Engine would hang indefinitely at the "Pulse Scan initiated" step. The issue was caused by `yf.download()` calls for `^NSEI` and `^INDIAVIX` lacking a `timeout` parameter, causing the main thread to block forever when the Yahoo Finance API became unresponsive. Added `timeout=15` to enforce failover.
* **Fix Upstox Portfolio UI Bugs**: Successfully resolved an issue where closed trades and open/in-progress trades were not reflecting correctly in the Upstox portfolio tab and intraday tracking cards. Diagnosed and fixed a hardcoded SQL prefix (`T-%` instead of `TRADE-%`) bug in `database_manager.py` that caused the frontend summary APIs to return empty trade sets. Enabled tracking of unrealized P&L on the top dashboard Intraday cards by expanding the SQL queries to include active `"OPEN"` positions.
* **Vanguard Engine Modular Refactoring**: Successfully completed the **[[01 — Architecture/Vanguard Engine Refactor Roadmap|Vanguard Engine Refactor Roadmap]]**, breaking down the 3,300+ line monolith into a clean, 9-module cohesive package under `scripts/vanguard/`. All imports, schedule scans, WebSocket integrations, and trade state machines are successfully isolated, and backward-compatible execution is verified.
* **Scripts Directory Restructuring**: Successfully organized and grouped 70+ scripts inside `scripts/` into modular subdirectories (`backtests/`, `training/`, `research/`, `collectors/`), keeping the `scripts/` root clean and aligned. Updated import references and verified zero system compilation or execution issues.
* **Codebase Streamlining & Phase 1/2 Cleanup**: Executed Phase 1 and 2 of the Codebase Cleanup Strategy. Successfully isolated 35 stale Python and Markdown files to `legacy_archive/`, updated the shared codebase index directory, and verified system compilation and dashboard snapshot APIs run with zero PyTorch or deprecated file dependencies.
* **Daily Gatekeeper Fix & PyTorch Cleanup**: Successfully pruned all retired daily temporal transformer PyTorch code from `vanguard_signal_engine.py`. Aligned the daily macro scan to the correct 165-feature `daily_xgb` schema, resolving a critical feature-mismatch bug and restoring true gatekeeper filters in live production.
* **Regime-Aware Routing**: Completed implementation and execution of `strategy_50_regime_backtest.py` across all 50 strategies, evaluating both gated and ungated passes for May 2026. Restored strategy-specific high-precision exits for top performers.
* **Market Psychology Indicators**: Architecting an intelligent layer that goes beyond technical analysis to quantify market psychology. Integrating order flow, volume profiles, and news sentiment into a predictive model to anticipate shifts in retail and institutional trader behavior ahead of momentum inflection points.
* **System Refinement**: Completed transition from Vanguard V1 static thresholds to V2.3 dynamic ATR-based logic, conviction scoring, and live UI strategy badges.
* **Codebase and Memory Layer Synchronization**: Successfully untracked cache directories to resolve repository bloat, aligned documentation files (`Codebase File Directory.md`, `Global System Architecture.md`, `Model Inference Data Structure.md`, etc.) to the new modular architecture, conducted a Gemini Pro audit, stripped dead tracker code from the AI veto logic, and pruned the `scratch/` directory of disposable one-off scripts.
* **30-Minute Model Complete Edge Research**: Conducted exhaustive 6-phase empirical analysis of the 30-minute XGBoost models via Jupyter MCP. Discovered the Long Model's edge concentrates at **15:15 IST** (60.2% WR, +32 bps at Score > 0.080), while the Short Model is structurally broken globally but has a narrow edge at **14:15 IST only** (56.3% WR). Signal inversions and Dual-Lock configurations provide negligible value — fundamentally different from the 1-hour model. Wrote 5 comprehensive reports with 7 visual assets to `08. Model Analysis/30-Minute Vanguard Model/`. See [[02 — Models/30m/Complete Edge Catalog|30-Min Edge Catalog]].
* **Training Data & Regime Overfitting Discovery**: Uncovered the structural reason why the 1-hour model succeeds and the 30-min/15-min models fail: the **Row Count Fallacy**. Despite having equivalent raw row counts (~540K), the 1-hour model was trained on 3 years of calendar data (multiple regimes), while the intraday models were trained on just 1 year. Established the mandate that **all Vanguard models must be trained on a minimum of 3 years of historical data** to prevent regime overfitting. Documented in [[02 — Models/_Shared/Training Data & Regime Requirements]].
* **Memory Integration**: Established this Obsidian vault as a shared memory layer for project continuity, updated with the latest 50-strategy backtest outcomes.
* **TradingView MCP Integration**: Cloned the open-source `tradesdontlie/tradingview-mcp` to enable reading live candles from the TradingView Desktop app via CDP. Documented setup steps in `07. MCP Integrations/TradingView MCP Setup.md`.
* **10:30 AM Momentum Strategy (V2 & V3)**: Completed implementation and testing of the V3 two-layer XGBoost strategy following the V2 forensic audit. V3 utilized a 4.5-year dataset (~1100 days) and vol-normalized features. Results conclusively confirm the model is noise: Layer A achieved 51.4% OOS accuracy, Layer B achieved 0.017 Spearman rho, and the best backtest threshold yielded flat returns with a -33.4% max drawdown. Standard tabular XGBoost models on intraday OHLCV cannot robustly predict 10:30 momentum returns. The project for Strategy 1030 has been shelved. Details in [[06 — Logs/Daily Logs/2026-06-03|Daily Log 2026-06-03]].
* **Jupyter MCP Connection Stabilized**: Resolved `403 Forbidden` connection failures in the `mcp-jupyter` sandbox. Diagnosed that the default port (8888) relies on token authentication which breaks `setup_notebook`. Updated the agent usage protocol in `[[05 — Operations/MCP/Jupyter MCP Setup]]` to mandate explicitly passing a tokenless `server_url` (e.g., `http://localhost:8889`) when invoking `setup_notebook`.
* **Jupyter MCP Connection Restored & Fixed ENV Wipout**: Identified that a previous agent's attempt to securely inject `JUPYTER_TOKEN` via `env` in `mcp_config.json` caused a Node.js PATH wipeout, resulting in `uvx` crashing and the MCP bridge returning an EOF error. The global `jupyter-notebook.exe` was also manually restarted on port `8888`. Removed the fatal `env` block from `mcp_config.json` and updated the protocol: agents must now authenticate by explicitly passing the token in the URL: `server_url="http://127.0.0.1:8888/?token=vanguard2026"`.
