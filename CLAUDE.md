# Project Memory

The full AI agent operating protocol and shared-memory guide for this repo lives in
`AGENTS.md` (a cross-tool standard shared with other AI coding agents). It is imported
below so Claude Code loads it automatically every session.

@AGENTS.md
